void hanoi(int n, string from, string to, string via) {
    if (n == 1) {
        print(from + " -> " + to);
        return;
    }
    hanoi(n - 1, from, via, to);
    print(from + " -> " + to);
    hanoi(n - 1, via, to, from);
}

int main() {
    hanoi(4, "A", "C", "B");
    return 0;
}
