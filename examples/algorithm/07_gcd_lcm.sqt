int gcd(int a, int b) {
    if (b == 0) { return a; }
    return gcd(b, a % b);
}


int main() {
    print(gcd(48, 18));
    print(gcd(1, 1));
    print(gcd(0, 5));

    try {
        int bad = 10 % 0;
        print(bad);
    } catch (Error e) {
        print(e.code);
    }


    return 0;
}
