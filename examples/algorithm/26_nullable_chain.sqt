int? safe_divide(int a, int b) {
    if (b == 0) { return null; }
    return a / b;
}

int main() {
    int? r = safe_divide(10, 3);
    if (r != null) {
        print(r);
    }

    int? zero = safe_divide(5, 0);
    if (zero == null) {
        print(1);
    }

    return 0;
}
