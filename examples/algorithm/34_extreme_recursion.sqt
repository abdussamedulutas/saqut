// Daha ağır fibonacci
int fib(int n) {
    if (n <= 1) { return n; }
    return fib(n - 1) + fib(n - 2);
}

int main() {
    // fib(35) ≈ 29 milyon çağrı
    // Timeout'a takılacak mı?
    print(fib(35));
    return 0;
}
