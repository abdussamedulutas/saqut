struct Point {
    int x;
    int y;
}

void setX(Point p, int val) {
    p.x = val;
}

int main() {
    Point p;
    p.x = 10;
    p.y = 20;
    print(p.x);
    print(p.y);
    setX(p, 99);
    print(p.x);
    return 0;
}
